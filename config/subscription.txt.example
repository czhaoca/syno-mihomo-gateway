# Format: Name=URL
# Copy this file to config/subscription.txt and paste your real Airport/Provider URL.
# config/subscription.txt is gitignored so your token never gets committed.
# The mihomo entrypoint uses the FIRST valid (non-#) line found.
# Example:
MyAirport=https://provider.example/api/v1/subscribe?token=REPLACE_ME
